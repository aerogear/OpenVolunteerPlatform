query findMyVolounteerActions($volounteerId: ID!, $status: ActionStatus) {
  findVolounteerActions(
    fields: { volounteerId: $volounteerId, status: $status }
  ) {
    ...VolounteerActionExpandedFields
  }
}
