query findActiveVolunteer($username: String!) {
  findVolunteers(fields: { username: $username }, limit: 1) {
    ...VolunteerFields
  }

  findVolounteerActions(
    fields: { volounteerId: $volounteerId }
    limit: $limit
    offset: $offset
  ) {
    ...VolounteerActionExpandedFields
  }
}
