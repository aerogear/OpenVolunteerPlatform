## NOTE: This schema was generated by Graphback and should not be changed manually



type Task {
  id: ID!
  title: String!
  description: String!
  status: TaskStatus
  version: Int
}

scalar ID

scalar String

enum TaskStatus {
  OPEN
  ASSIGNED
  COMPLETE
}

scalar Int

input TaskInput {
  id: ID = null
  title: String = null
  description: String = null
  status: TaskStatus = null
  version: Int = null
}

type Query {
  findAllTasks: [Task]!
  findTasks(fields: TaskInput = null): [Task]!
} 

type Mutation {
  createTask(input: TaskInput = null): Task!
  updateTask(input: TaskInput = null): Task!
  deleteTask(input: TaskInput = null): Task!
} 

type Subscription {
  newTask(input: TaskInput = null): Task!
  updatedTask(input: TaskInput = null): Task!
  deletedTask(input: TaskInput = null): Task!
} 

